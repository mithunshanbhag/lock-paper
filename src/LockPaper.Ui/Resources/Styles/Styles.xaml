<?xml version="1.0" encoding="UTF-8" ?>
<?xaml-comp compile="true" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Thickness x:Key="ScreenPadding">24</Thickness>

    <Style x:Key="DeviceScreenStyle"
           TargetType="Border">
        <Setter Property="Background"
                Value="{StaticResource ScreenBackgroundBrush}" />
        <Setter Property="Padding"
                Value="22,28" />
        <Setter Property="Stroke"
                Value="{StaticResource ScreenFrameColor}" />
        <Setter Property="StrokeThickness"
                Value="12" />
        <Setter Property="StrokeShape"
                Value="RoundRectangle 36" />
        <Setter Property="Shadow">
            <Setter.Value>
                <Shadow Brush="#2E2D4B"
                        Opacity="0.28"
                        Radius="28"
                        Offset="0,16" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AppTitleLabelStyle"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="36" />
        <Setter Property="FontAttributes"
                Value="Bold" />
        <Setter Property="TextColor"
                Value="{StaticResource TitleTextColor}" />
    </Style>

    <Style x:Key="StatusIconButtonStyle"
           TargetType="Button">
        <Setter Property="WidthRequest"
                Value="40" />
        <Setter Property="HeightRequest"
                Value="40" />
        <Setter Property="CornerRadius"
                Value="20" />
        <Setter Property="FontAttributes"
                Value="Bold" />
        <Setter Property="FontSize"
                Value="18" />
        <Setter Property="BackgroundColor"
                Value="{StaticResource SuccessSurfaceColor}" />
        <Setter Property="TextColor"
                Value="{StaticResource SuccessTextColor}" />
        <Setter Property="Padding"
                Value="0" />
    </Style>

    <Style x:Key="PrimaryActionButtonStyle"
           TargetType="Button">
        <Setter Property="HeightRequest"
                Value="56" />
        <Setter Property="CornerRadius"
                Value="16" />
        <Setter Property="BackgroundColor"
                Value="{StaticResource PrimaryActionColor}" />
        <Setter Property="TextColor"
                Value="White" />
        <Setter Property="FontAttributes"
                Value="Bold" />
        <Setter Property="FontSize"
                Value="16" />
        <Setter Property="Shadow">
            <Setter.Value>
                <Shadow Brush="{StaticResource PrimaryActionPressedColor}"
                        Opacity="0.24"
                        Radius="20"
                        Offset="0,10" />
            </Setter.Value>
        </Setter>
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="Opacity"
                                    Value="0.7" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="BaseCardStyle"
           TargetType="Border">
        <Setter Property="Background"
                Value="{StaticResource CardBackgroundColor}" />
        <Setter Property="Padding"
                Value="16" />
        <Setter Property="Stroke"
                Value="{StaticResource CardBorderColor}" />
        <Setter Property="StrokeShape"
                Value="RoundRectangle 18" />
        <Setter Property="StrokeThickness"
                Value="1" />
    </Style>

    <Style x:Key="NoticeCardStyle"
           TargetType="Border"
           BasedOn="{StaticResource BaseCardStyle}" />

    <Style x:Key="StatusCardStyle"
           TargetType="Border"
           BasedOn="{StaticResource BaseCardStyle}" />

    <Style x:Key="PlaceholderCardStyle"
           TargetType="Border"
           BasedOn="{StaticResource BaseCardStyle}" />

    <Style x:Key="SectionLabelStyle"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="12" />
        <Setter Property="FontAttributes"
                Value="Bold" />
        <Setter Property="CharacterSpacing"
                Value="1.5" />
        <Setter Property="TextTransform"
                Value="Uppercase" />
        <Setter Property="TextColor"
                Value="{StaticResource MutedTextColor}" />
    </Style>

    <Style x:Key="CardTitleLabelStyle"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="20" />
        <Setter Property="FontAttributes"
                Value="Bold" />
        <Setter Property="TextColor"
                Value="{StaticResource CardTextColor}" />
    </Style>

    <Style x:Key="CardBodyLabelStyle"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="15" />
        <Setter Property="LineHeight"
                Value="1.4" />
        <Setter Property="TextColor"
                Value="{StaticResource MutedTextColor}" />
    </Style>

    <Style x:Key="CardValueLabelStyle"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="17" />
        <Setter Property="FontAttributes"
                Value="Bold" />
        <Setter Property="LineHeight"
                Value="1.3" />
        <Setter Property="TextColor"
                Value="{StaticResource CardTextColor}" />
    </Style>

    <Style x:Key="PlaceholderPickerStyle"
           TargetType="Picker">
        <Setter Property="BackgroundColor"
                Value="White" />
        <Setter Property="TextColor"
                Value="{StaticResource CardTextColor}" />
        <Setter Property="TitleColor"
                Value="{StaticResource MutedTextColor}" />
    </Style>
</ResourceDictionary>
